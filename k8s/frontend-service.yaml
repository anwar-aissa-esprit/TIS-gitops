apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: tis-app
  labels:
    app: tis-frontend
    managed-by: argocd
spec:
  type: ClusterIP
  selector:
    app: tis-frontend
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP