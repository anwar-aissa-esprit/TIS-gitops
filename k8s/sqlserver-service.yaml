apiVersion: v1
kind: Service
metadata:
  name: sqlserver-service
  namespace: tis-app
  labels:
    app: sqlserver
    managed-by: argocd
spec:
  type: ClusterIP
  selector:
    app: sqlserver
  ports:
  - name: mssql
    port: 1433
    targetPort: mssql
    protocol: TCP